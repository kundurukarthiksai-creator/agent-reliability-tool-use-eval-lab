# GitHub Metadata

Suggested repository description:

```text
Deterministic evaluation lab for tool-using AI agents with traces, scoring, HTML reports, and SQLite run history.
```

Suggested topics:

```text
ai-agents
agent-evaluation
tool-use
evals
fastapi
pydantic
sqlite
pytest
developer-tools
reliability
```

Suggested pinned-repo framing:

```text
Built a CI-safe lab for evaluating whether AI agents choose the right tools, produce supported outputs, and expose failures through traces and assertion-level reports.
```

Do not claim:

- production usage;
- benchmark leadership;
- LLM generalization;
- employer/internal validation.
